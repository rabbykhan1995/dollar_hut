import RegisterForm from "@/components/RegisterForm/RegisterForm";
import React from "react";

const page = () => {
  return (
    <div>
      <RegisterForm />
    </div>
  );
};

export default page;
